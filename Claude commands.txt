Persoonlijke commandonotities - nooit echte secrets in dit bestand zetten.

Repository:
C:\Users\Gebruiker\Documents\Legal Baseline Digidactics\rss-ai-analyst-legal-worktree

Status controleren:
git status --short --branch
git diff --stat

Wijzigingen bewust stagen:
git add <bestand-of-map>

Commit maken:
git commit -m "<beschrijvende-commitboodschap>"

Pushen:
git push -u origin codex/legal-pipeline

Cron handmatig triggeren:

$cronSecret = "<CRON_SECRET_UIT_PASSWORD_MANAGER>"
$headers = @{ Authorization = "Bearer $cronSecret" }

Invoke-WebRequest `
  -Uri "https://rss-ai-analyst.vercel.app/api/cron/daily-brief" `
  -Method GET `
  -Headers $headers `
  -UseBasicParsing

Invoke-WebRequest `
  -Uri "https://rss-ai-analyst.vercel.app/api/cron/newsletter-draft" `
  -Method GET `
  -Headers $headers `
  -UseBasicParsing

Invoke-WebRequest `
  -Uri "https://rss-ai-analyst.vercel.app/api/cron/legal-scan" `
  -Method GET `
  -Headers $headers `
  -UseBasicParsing

Voer geen DELETE-, migration- of productiecommando uit vanuit deze notities zonder aparte review.
